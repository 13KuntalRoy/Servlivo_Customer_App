// Removed: the availability/slots use case was never wired to any screen and the
// backend exposes no per-service slot endpoint. Re-introduce alongside a real
// `/catalog/availability` slot API if scheduling-by-slot is added.
