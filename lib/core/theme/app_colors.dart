import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFFFF6B2C);
  static const Color secondary = Color(0xFF2DC7A4);

  // Backgrounds
  static const Color background = Color(0xFFFFF8F5);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFFB0B0B0);
  static const Color textOnPrimary = Colors.white;

  // Status
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFFA726);
  static const Color info = Color(0xFF1E88E5);

  // Booking status badge colors
  static const Color statusUpcoming = Color(0xFF1E88E5);
  static const Color statusOngoing = Color(0xFF2DC7A4);
  static const Color statusCompleted = Color(0xFF43A047);
  static const Color statusCancelled = Color(0xFFE53935);

  // UI elements
  static const Color divider = Color(0xFFEEEEEE);
  static const Color border = Color(0xFFDDDDDD);
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);
  static const Color shadow = Color(0x14000000);

  // Prime gradient
  static const Color primeGradientStart = Color(0xFFFF6B2C);
  static const Color primeGradientEnd = Color(0xFFFF9A5C);

  // Rating star
  static const Color star = Color(0xFFFFC107);

  // Verified badge
  static const Color verified = Color(0xFF2DC7A4);

  // Overlay
  static const Color overlay = Color(0x80000000);
}
